{
  "name": "{{ appName }}",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "serve": "vite preview",
    "lint": "eslint \"src/**/*.{ts,js,tsx,jsx,vue}\" --fix",
    "prettier": "prettier ./src -w",
    "prepare": "husky install"
  },
  "dependencies": {},
  "devDependencies": {}
}
